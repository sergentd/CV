# import necessary packages
import argparse

# construct the argument parser and parse the arguments
ap = argpase.ArgumentParser()
ap.add_argument("-","--", required=True, help="")
args = vars(ap.parse_args())